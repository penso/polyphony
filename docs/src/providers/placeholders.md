# Placeholders And Empty Dirs

Several repository directories currently exist without code or workspace membership:

- `crates/polyphony-agent-claude`
- `crates/polyphony-agent-copilot`
- `crates/polyphony-agent-openai`
- `crates/factoryrs-runtime`

These directories appear to reserve space for future provider-specific runtimes and a broader
runtime extraction, but they are not active parts of the current build.
